# TRIBE v2 — installed from my fork with relaxed torch upper bound
tribev2[plotting] @ git+https://github.com/rohany395/tribev2

# neuralset imports exca.steps.base.NoValue; removed in exca 0.5.26 (tribev2#69)
exca>=0.5.20,<0.5.26

# PyTorch (compatible with ZeroGPU Blackwell)
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0

# ZeroGPU support
spaces>=0.19.4

# Headless visualization
matplotlib>=3.8.0
plotly>=5.18.0

# HF auth
huggingface_hub>=0.23.0

# WhisperX stack (tribev2_whisperx_patch.py installs whisperx + faster-whisper --no-deps).
# whisperx 3.1.5 requires faster-whisper==1.0.1 (API incompatible with 1.2+).
# faster-whisper 1.0.1 pins av==11.* (fails to compile on FFmpeg 7); av 12.3 has wheels.
av==12.3.0
ctranslate2>=4.0,<5
onnxruntime>=1.14,<2
tokenizers>=0.13,<0.16
pyannote.audio==3.3.2
nltk>=3.8.0
transformers>=4.30.0
pandas>=2.0.0

# Brain atlas
nilearn>=0.11.0
