# TRIBE v2 — installed from my fork with relaxed torch upper bound
tribev2[plotting] @ git+https://github.com/rohany395/tribev2

# neuralset imports exca.steps.base.NoValue; removed in exca 0.5.26 (tribev2#69)
exca>=0.5.20,<0.5.26

# PyTorch (compatible with ZeroGPU Blackwell)
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0

# ZeroGPU support
spaces>=0.19.4

# Headless visualization
matplotlib>=3.8.0
plotly>=5.18.0

# HF auth
huggingface_hub>=0.23.0

# WhisperX stack (tribev2_whisperx_patch.py installs whisperx --no-deps).
# PyAV 14+ ships cp312 wheels; faster-whisper 1.2+ allows av>=11.
# pyannote 3.3.2 works with torchaudio 2.8 and numpy 2.x (3.1.1 does not).
av>=14.2.0,<15
faster-whisper>=1.2.0,<2
pyannote.audio==3.3.2
nltk>=3.8.0
transformers>=4.30.0
pandas>=2.0.0

# Brain atlas
nilearn>=0.11.0
