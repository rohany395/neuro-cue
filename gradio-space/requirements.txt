# TRIBE v2 — installed from my fork with relaxed torch upper bound
tribev2[plotting] @ git+https://github.com/rohany395/tribev2

# neuralset imports exca.steps.base.NoValue; removed in exca 0.5.26 (tribev2#69)
exca>=0.5.20,<0.5.26

# PyTorch (compatible with ZeroGPU Blackwell)
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0

# ZeroGPU support
spaces>=0.19.4

# Headless visualization
matplotlib>=3.8.0
plotly>=5.18.0

# HF auth
huggingface_hub>=0.23.0

# tribev2 uses WhisperX via uvx at runtime (tribev2_whisperx_patch.py).
# Do NOT pip-install whisperx here: faster-whisper pins av==11.* which fails to
# compile against FFmpeg 7 on Debian Trixie during the Space image build.
uv>=0.4.0

# Brain atlas
nilearn>=0.11.0