# Copy to .env.local (gitignored).
#
# Vite exposes every VITE_* value in the browser bundle. Do not put Hugging Face
# tokens or shared API secrets here; the React app calls the public Space.

# Public Space URL - browser uploads video and runs predictions here.
VITE_SPACE_URL=https://rohany395-neuro-cue.hf.space/
