# Copy to .env.local (gitignored).
#
# Public Space URL. Only VITE_* values are bundled into browser JavaScript; do
# not put HF_TOKEN, PREDICT_API_SECRET, or any other server secret here.
VITE_SPACE_URL=https://rohany395-neuro-cue.hf.space/
