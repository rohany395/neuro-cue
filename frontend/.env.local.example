# Copy to .env.local (gitignored) for optional local overrides.

# Optional: call a different public Gradio Space from the browser.
# Do not put Hugging Face tokens in Vite env vars; VITE_* values are bundled.
# VITE_SPACE_URL=https://rohany395-neuro-cue.hf.space/
