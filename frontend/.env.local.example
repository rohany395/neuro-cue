# Copy to .env.local (gitignored).
#
# Public Space URL. The browser calls this Space directly for both prediction
# and video uploads, so do not put private Hugging Face tokens in VITE_* vars.
VITE_SPACE_URL=https://rohany395-neuro-cue.hf.space/
